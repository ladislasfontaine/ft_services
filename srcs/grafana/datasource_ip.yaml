apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: telegraf
    user: telegraf
    password: secret
    url: http://REPLACE_IP:8086
    jsonData:
      httpMode: GET